def add( datatype, *args):
    if( datatype=='int'):
        answer='0'
    if( datatype=='str'):
        answer=''
    for *args in x:
        answer=answer+x
    print(answer)
add('int',5,100)
add('str','good','section')
